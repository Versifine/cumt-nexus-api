# App
APP_NAME=cumt-nexus-api
APP_ENV=local
APP_STARTUP_TIMEOUT=10s

# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DATABASE=cumt_nexus
POSTGRES_SSL_MODE=disable
POSTGRES_MAX_CONNS=25
POSTGRES_MAX_CONN_LIFETIME=5m
POSTGRES_MAX_CONN_IDLE_TIME=2m

# HTTP
HTTP_ADDR=:8080
HTTP_READ_TIMEOUT=5s
HTTP_WRITE_TIMEOUT=10s
HTTP_SHUTDOWN_TIMEOUT=15s
HTTP_CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Log
LOG_LEVEL=info
LOG_FORMAT=json

# Auth
AUTH_TOKEN_SECRET=MyAuthSecret
AUTH_ACCESS_TOKEN_TTL=24h
AUTH_EMAIL_ALLOWED_DOMAINS=cumt.edu.cn,mail.cumt.edu.cn
AUTH_EMAIL_CODE_TTL=10m
AUTH_EMAIL_CODE_RESEND_INTERVAL=1m
AUTH_EMAIL_CODE_MAX_ATTEMPTS=5
AUTH_EMAIL_CODE_DAILY_LIMIT=10
AUTH_EMAIL_CODE_IP_HOURLY_LIMIT=30
AUTH_EMAIL_CODE_LENGTH=6

# Mail
MAIL_PROVIDER=log
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_TLS_MODE=starttls

# Object storage
# Production uses Cloudflare R2. Keep local only for local no-credential smoke.
OBJECT_STORAGE_PROVIDER=local
OBJECT_STORAGE_ENDPOINT=
OBJECT_STORAGE_REGION=auto
OBJECT_STORAGE_BUCKET=
OBJECT_STORAGE_ACCESS_KEY_ID=
OBJECT_STORAGE_SECRET_ACCESS_KEY=
OBJECT_STORAGE_PUBLIC_BASE_URL=http://localhost:8080/uploads
OBJECT_STORAGE_FORCE_PATH_STYLE=true
OBJECT_STORAGE_LOCAL_ROOT=var/uploads

# Upload
UPLOAD_IMAGE_MAX_BYTES=5242880
UPLOAD_IMAGE_MAX_COUNT_PER_POST=9
UPLOAD_IMAGE_MAX_COUNT_PER_COMMENT=1
